import Foundation

enum APIConfig {
    static let baseURL = "https://shiye-app.onrender.com"

    static let identifyEndpoint = "\(baseURL)/api/identify"
    static let artworkEndpoint = "\(baseURL)/api/artwork"
}
